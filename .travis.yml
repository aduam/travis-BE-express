language: node_js

cache:
  yarn: true
  directories:
    - node_modules
    - ~/.npm

node_js:
  - '14'

git:
  depth: 3

script:
  - yarn test

notifications:
  email:
    recipients:
      - aduartem@miumg.edu.gt
      - alan@warava.com
  on_success: always
  on_failure: always

deploy:
  provider: heroku
  skip_cleanup: true
  keep_history: true
  api_key: $HEROKU_KEY
  app: travis-be-express
  on:
    branch: master